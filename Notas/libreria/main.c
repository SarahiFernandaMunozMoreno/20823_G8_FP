#include <stdio.h>
#include "mimath.h"

int main() {
    float a = 5.0, b = 3.0;
    printf("Suma: %.2f\n", suma(a, b));
    printf("Resta: %.2f\n", resta(a, b));
    printf("Multiplicación: %.2f\n", multiplicacion(a, b));
    return 0;
}
