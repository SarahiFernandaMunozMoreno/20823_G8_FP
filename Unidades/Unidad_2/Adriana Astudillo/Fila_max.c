# include <stdio.h>
#define max
int main () {
int i,j;
int f,c;
int n;
int max;
int mat[f][c];
max=0
//numero de filas y columnas
printf("Ingrese el numero de filas: ");
    scanf("%d", &f);
pritf("Ingrese el numero de columnas",);
    scanf("%d", &c);
//Matriz generada
    for (i = 0; i < f; i++) {
        for (j = 0; j < c; j++) {
            matriz[i][j] = 0;
            printf("%d ", matriz[i][j]);
        }
        printf("\n");
    }
//Ingrsar fila
printf("Ingrese la fila i: ",f-1);
    scanf("%d", &f_i);
    if (i<=0, i<f)
        max=

    }

}
    }
