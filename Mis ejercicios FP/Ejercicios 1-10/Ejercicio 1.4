#include <stdio.h>
int main() {
    float n_1;
    printf("Ingresa un numero entero mayor a 0: \n");
    scanf("%f", &n_1);
    if ((n_1 >= 0) && (n_1 < 10)) {
        printf("Si esta en el intervalo\n");
    } else {
        printf("No pertenece al intervalo\n");
    }
    return 0;
}
