#include <stdio.h>

int main() {
    double N_1, N_2, M;
    
    printf("Ingresa un primer numero real: ");
    scanf("%lf", &N_1);
    
    printf("Ingresa el segundo numero real: ");
    scanf("%lf", &N_2);
    
    M = (N_1 + N_2) / 2;
    
    printf("La media entre ellos es: %lf\n", M);
    
    return 0;
}
