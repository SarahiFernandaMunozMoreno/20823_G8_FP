#include <stdio.h>
#include <math.h>

int main() {
    float N_1, i;
    printf("Ingresar un entero mayor a 0: ");
    scanf("%f", &N_1);
    i = sqrt(N_1);
    if (i > 1) {
        printf("La Raiz es: %.2f\n", i);
    }
    return 0;
}
